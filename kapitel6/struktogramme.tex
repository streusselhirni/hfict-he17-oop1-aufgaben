\documentclass{article}
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage{struktex}

\begin{document}

    \section{Aufgabe 6.1}
    \begin{struktogramm}(150,50)
        \assign{$m_1$ einlesen}
        \assign{$m_2$ einlesen}
        \assign{$r$ einlesen}
        \assign{Rechne $f \times \frac{m_1 \times m_2}{r^2}$}
        \assign{Gib Ergebnis aus}
    \end{struktogramm}

    \section{Aufgabe 6.2}
    \begin{struktogramm}(150,50)
        \assign{Startwert $s$ einlesen}
        \assign{Endwert $e$ einlesen}
        \assign{Schrittweite $w$ einlesen}
        \while{Solange $s <= e$}
            \assign{Aktuelles $s$ ausgeben}
            \assign{(Start-)Wert $s$ um Schrittweite $w$ erhöhen}
        \whileend
    \end{struktogramm}

\end{document}